class ArtistModel {
    constructor(id, name, genres, members, website, image) {
      this.id = id;
      this.name = name;
      this.genres = genres;
      this.members = members;
      this.website = website;
      this.image = image;
      // this.albums = []; // You can add albums to this array later
    }
  }
  
  export default ArtistModel;
  